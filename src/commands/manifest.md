# /manifest

Purpose: Inspect managed framework files, local edits, missing files, preserved paths, profile, adapter, and manifest metadata.

Outputs:

* managed-file count
* local-edit count
* missing-file count
* adapter and profile summary

Example usage:

```text
cubby manifest --workspace ./examples/k5-special-ed-workspace
```
