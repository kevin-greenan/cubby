# Adapter Contract

Adapters translate Cubby's provider-neutral source files into provider-specific installed workspace files.

The current local release implements only the Codex adapter in `src/adapters/codex/`.
